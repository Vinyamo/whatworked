# Study outputs are personal — never commit them
*_study_*/
study_*.md
study_*.pdf
# study_*.md matches STUDY_GUIDELINES.md when core.ignorecase=true (macOS default)
!STUDY_GUIDELINES.md
discovery_*.json
diagnostics_*.json
passing_records.json
transcripts/
notes/

# Credentials / env — never commit
.env
.env.*
!.env.example
.studyd_credentials
*credentials*

# OS / editor / python noise
.DS_Store
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
